import 'dart:ui';

import 'package:spritewidget/spritewidget.dart';

enum TowerColor {
  Red,
  Blue
}

class Tower extends NodeWithSize{
  Tower(ImageMap map, Node rootNode, TowerColor color)
    : super(Size.zero) {
      var foo = 0;
  }
}